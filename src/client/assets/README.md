# Generated Art

`src/client/assets/generated/` is the output folder for OpenAI-generated pixel assets.

Run:

```bash
npm run assets:generate
```

Required environment variables:

- `OPENAI_API_KEY`

Optional:

- `OPENAI_IMAGE_MODEL` defaults to `gpt-image-1.5`
